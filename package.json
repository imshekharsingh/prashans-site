{
  "name": "prashans-site",
  "version": "1.0.0",
  "description": "Static site with Cloudflare features",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}